using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TiposVariables : MonoBehaviour
{
    int integerVariable; //... -3, -2, -1, 0, 1, 2, 3, ... (valores comprendidos entre -2,147,483,648 y 2,147,483,647)
    float realVariable; //3.14159265f, 6.28f, -4.73f (valores comprendidos entre ±1.5 x 10^-45 y ±3.4 x 10^38)
    string stringVariable = "Hola me llamo Juan Alberto"; //cadena de caracteres
    char characterVariable = 'a'; // 'a', 'b', '7', '@', '#', '$'
    bool booleanVariable; //true or false, si la declaramos sin valor, por defecto es false

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
